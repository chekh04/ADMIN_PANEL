import { AdminButtonDirective } from './admin-button.directive';

describe('AdminButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminButtonDirective();
    expect(directive).toBeTruthy();
  });
});
