import { AdminInputDirective } from './admin-input.directive';

describe('AdminInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminInputDirective();
    expect(directive).toBeTruthy();
  });
});
